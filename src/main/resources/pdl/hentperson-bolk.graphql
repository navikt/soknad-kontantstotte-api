query($ident: [ID!]!) {
    hentPersonBolk(ident: $ident) {
        ident,
        person {
            foedsel {
                foedselsdato
            }
            navn {
                fornavn
                mellomnavn
                etternavn
            }
        }
    }
}